import Foundation

// Fixture: the legitimate invocation site — the user-initiated Restart click.
final class RestartMenuAction {
    let coordinator = UpdaterCoordinator()

    func restartClicked() {
        coordinator.installDownloadedUpdateAndRelaunch()
    }
}
